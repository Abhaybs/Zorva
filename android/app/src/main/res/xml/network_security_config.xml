<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Development only: allow HTTP to emulator host and LAN backend. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
